// Внешняя обработка "Загрузка ГСМ из Excel"
// Файл создан для определения языка 1C Enterprise в GitHub

Перем xB, НачДата, КонДата, НЗ;

Процедура Конструктор()
	ПоследняяОшибка = "";
	Загружена = 0;
	Лист = "";
КонецПроцедуры

Процедура Деструктор()
	Если Строка(ТипЗнч(Эксель)) = "COM Объект" Тогда
		Эксель.Workbooks.close();
		Эксель.Quit();
		Эксель = "";
	КонецЕсли;
КонецПроцедуры
